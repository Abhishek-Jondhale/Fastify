// import { FastifyInstance } from 'fastify';
// import {
//   createCandidate,
//   getCandidates,
//   getCandidateById,
//   updateCandidate,
//   deleteCandidate,
// } from '../controllers/candidateControlllers';

// const candidateRoutes = async (fastify: FastifyInstance) => {
//   fastify.post('/', createCandidate);
//   fastify.get('/', getCandidates);
//   fastify.get('/:id', getCandidateById);
//   fastify.put('/:id', updateCandidate);
//   fastify.delete('/:id', deleteCandidate);
// };

// export default candidateRoutes;
