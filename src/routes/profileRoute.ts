// import { FastifyInstance } from 'fastify';
// import * as CampaignController from '../controllers/campaignControllers';

// export default async function (fastify: FastifyInstance) {
//   // POST route to create a new campaign
//   fastify.post('/api/campaigns', (request, reply) => CampaignController.createCampaign(fastify, request, reply));

//   // GET route to fetch all campaigns
//   fastify.get('/api/campaigns', (request, reply) => CampaignController.getCampaigns(fastify, request, reply));
// }
