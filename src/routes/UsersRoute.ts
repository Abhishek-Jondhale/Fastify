// import { FastifyPluginAsync } from 'fastify';
// import { createUser, getAllUsers, getUsersWithOrders } from '../controllers/UsersController';
// import { createUserSchema } from '../models/UserModel';

// const userRoutes: FastifyPluginAsync = async (fastify) => {
//     fastify.post('/users', { schema: createUserSchema }, createUser);
//     fastify.get('/users', getAllUsers);
//     fastify.get('/users-with-orders', getUsersWithOrders);
// };

// export default userRoutes;
